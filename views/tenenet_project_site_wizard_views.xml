<odoo>
    <data>
        <record id="view_tenenet_project_site_wizard_form" model="ir.ui.view">
            <field name="name">tenenet.project.site.wizard.form</field>
            <field name="model">tenenet.project.site.wizard</field>
            <field name="arch" type="xml">
                <form string="Pridať prevádzky, centrá alebo terén">
                    <sheet>
                        <group>
                            <field name="project_id" readonly="1"/>
                            <field name="site_type"/>
                            <field name="available_site_ids" invisible="1"/>
                            <field name="site_ids"
                                   widget="many2many_tags"
                                   domain="[('id', 'in', available_site_ids)]"
                                   options="{'no_create': True}"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_confirm"
                                type="object"
                                string="Pridať"
                                class="btn-primary"/>
                        <button string="Zrušiť" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
